import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Student Organization Member List',
  webDir: 'dist'
};

export default config;
